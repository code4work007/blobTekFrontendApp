# Application
VITE_APP_NAME=SaaS Platform

# API Configuration
# Replace with your backend base URL (no trailing slash)
VITE_API_BASE_URL=https://api.example.com/v1

# Set to "true" to load mock fixtures instead of real API calls
VITE_USE_MOCK=false
